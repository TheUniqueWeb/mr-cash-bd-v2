// Vercel Serverless Function entry point placeholder
// This file will be overwritten by the build script with the bundled server
export default function handler(req, res) {
  res.status(200).json({ message: "Server is booting..." });
}
